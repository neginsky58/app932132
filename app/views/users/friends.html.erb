<div class="row">
  <h1>Friends</h1>

  <% @friends.each do |friend| %>
    <div class="col-xs-6 col-md-3">
      <a href="#" class="thumbnail">
        <img src="<%= friend[:picture] %>" alt="<%= friend['name'] %>" width="100" height="100" >
      </a>
    </div>

  <% end %>

</div>